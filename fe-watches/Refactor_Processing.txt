- Add Hooks, Components. Chú ý các components nâng cao với các trang có filter, có thống kê.
- Refactored with hooks and component: StorePage, EmployeesPage, OrderPage, PermissionPage, UserPage, GroupPage, 
- Với AccessControl, đặt dòng này ở sau các hook và useCallBack, useEffect:
 // Sau khi khai báo hook, mới kiểm tra quyền
  if (!hasAccess) {
    return (
      <div className="admin-stores-list">
        <AccessDeniedAlert 
          hasAccess={hasAccess} 
          module="store"
          action="view"
          showUserInfo={true}
        />
      </div>
    );
  }
- Các trang đã được cập nhật HasAccess: StorePage, EmployeePage, GroupPage, OrderPage, 
- Các trang đã được cập nhật hook useListData (mới): GroupPage, PermissionPage, UserListPage, StorePage

RẤT NHIỀU TRANG ĐANG BỊ LỖI API ENDPOINT VÌ ĐÃ CẤU HÌNH LẠI CÁC API TRONG API.JS . TIẾN HÀNH SỬA KHI REFACTOR CÁC TRANG ĐÓ.